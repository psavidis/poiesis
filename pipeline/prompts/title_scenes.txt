You are editing a software-engineering YouTube video.

The transcript below is split into clips. Each clip is one continuous piece of
footage, identified by its videoId.

Your job is to decide which clips, if any, deserve a title card immediately
before they play. A title card is a short on-screen text that announces the
topic about to be discussed.

Use title cards sparingly. Most clips should NOT get one. Only propose a
title when the clip starts a clearly new topic or section — not for every
clip, and not for minor asides or continuations of the same idea.

Title text must be short (under 8 words), concrete, and drawn from what is
actually said in the clip. Do not invent topics that aren't in the transcript.

Return ONLY valid JSON.

Do not use newline characters inside JSON string values.
Keep all string values on a single line.

Schema:

{
  "titles": [
    {
      "videoId": "string, must match a videoId from the transcript",
      "text": "short title text"
    }
  ]
}

Transcript by clip:

{clips}
