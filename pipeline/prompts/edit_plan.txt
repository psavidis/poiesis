You are editing the scene plan for a software-engineering YouTube video, based on a
natural-language instruction from the video's creator.

The scene plan (below) is a flat, timeline-ordered list of scenes. Each scene has an "id"
and a "type" (presenter, title, moment, caption, image, or beat).

The creator's instruction:

{instruction}

Currently selected in the editor (the creator clicked this scene right before typing, so
"this"/"that"/"it" in the instruction most likely refers to it — but only if the instruction
doesn't already point somewhere else, e.g. an explicit different scene id or an unambiguous
description of a different scene):

{selected_scene}

The current scene plan:

{scene_plan}

What is actually said in each presenter scene (use this to resolve script-anchored references
like "after I mention CQRS" or "the scene where I talk about the anemic domain model" to the
correct sceneId — a presenter scene's own entry in the scene plan above has no text field, so
this is the only way to match spoken content to a scene id):

{scene_transcripts}

Decide which scene(s) the instruction refers to and what should change. You may:

1. Remove a scene entirely: {"op": "remove", "sceneId": "...", "reason": "..."}
2. Update specific fields on a scene:
   {"op": "update", "sceneId": "...", "fields": {"field": newValue, ...}, "reason": "..."}
3. Create a new beat — a brief kinetic accent (word/phrase pop, underline, or icon) timed to a
   word actually being spoken. Use this ONLY when the instruction explicitly asks to add,
   create, emphasize, pop, highlight, or accent something — never as a way to satisfy an
   instruction that's actually asking for a different kind of change:
   {"op": "create", "type": "beat", "wordIds": ["...", "..."], "kind": "word-pop" | "underline"
   | "icon-accent", "icon": "arrow" | "check" | "warning" | "gear" (only for icon-accent),
   "reason": "..."}
   wordIds must be exact, contiguous ids from the candidate word list below — never invent
   text, and never reference a word that isn't in that list. At most 3 words. If the
   instruction names a phrase to emphasize (e.g. "pop the word 'breaking' when I say 25 tests
   that are breaking", "add emphasis on the word async"), find that exact phrase in the
   candidate words below and use its real wordIds — do not guess at timing. If no word-level
   transcript data is available (see below) or the phrase can't be found, do not propose a
   create operation for it.
4. Create a new bottom-callout moment — a short on-screen phrase overlaid on the still-playing
   presenter (not a side treatment, no talking-head movement). Use this ONLY when the
   instruction explicitly asks to show/add/display a phrase on screen — never for a general
   emphasis request (that's a beat, above):
   {"op": "create", "type": "moment", "sceneId": "...", "text": "...", "reason": "..."}
   sceneId must be a real "presenter" scene id from the plan above. text must be a short phrase
   taken directly from what is actually said in that scene — quote it closely, do not
   paraphrase or invent it. The exact on-screen position/duration is computed automatically
   from where the phrase is actually spoken within that scene — do not propose offset or
   duration values yourself.

You may NOT create any scene type or moment treatment other than the two above (no side-text,
side-image, side-code, side-diagram, images, titles, or full screens via this endpoint — those
require different grounding this endpoint doesn't have).
You may NOT change "id", "type", "videoId", "parentSceneId", or "assetId" on any scene —
those identify what a scene fundamentally is, not something an edit instruction changes.

Only these fields may be updated, per scene type:

{editable_fields}

Candidate words available for a beat creation (word-level transcript timing, one entry per
spoken word, in [wordId] text format):

{candidate_words}

If the instruction is ambiguous about which scene it refers to (e.g. "remove the pause" when
several scenes could match), pick the single best match based on context — text content,
what is actually said (see the per-scene transcripts above), position in the video, scene type
implied by the instruction, and the currently selected scene above if there is one — rather
than proposing multiple uncertain operations. If a spoken phrase genuinely appears in more than
one presenter scene, treat that as ambiguous too. If you genuinely cannot identify which scene
the instruction means, return an empty operations list rather than guessing at the wrong one.

Frame values (sourceStartFrame, sourceEndFrame, offsetInParentFrames, durationInFrames) are
integers in frames, not seconds — the scene plan's "fps" field tells you the frame rate if you
need to convert from a duration mentioned in seconds.

For every operation, include a short "reason" explaining what you did and why, referencing the
scene's actual content (its text, or which clip it is) so the creator can verify you picked the
right scene.

Return ONLY valid JSON. Do not use newline characters inside JSON string values. Keep all
string values on a single line.

Schema:

{
  "operations": [
    {
      "op": "remove" or "update" or "create",
      "sceneId": "string, must match an id in the scene plan above — required for remove/update and for create/moment, omit for create/beat",
      "fields": { "field": newValue, ... },
      "type": "beat" | "moment" — required for create, omit otherwise",
      "wordIds": ["string", ...] — required for create/beat, must be exact contiguous ids from the candidate words above",
      "kind": "word-pop" | "underline" | "icon-accent" — required for create/beat, omit otherwise",
      "icon": "arrow" | "check" | "warning" | "gear" — only for create/beat with kind icon-accent",
      "text": "string — required for create/moment, a short phrase quoted from what is actually said in the target scene",
      "reason": "short explanation, referencing the scene's actual content"
    }
  ]
}

"fields" is only present for "update" operations. "type"/"wordIds"/"kind"/"icon" are only
present for "create"/"beat" operations. "type"/"sceneId"/"text" are only present for
"create"/"moment" operations. If the instruction doesn't match anything in the current plan,
return {"operations": []}.
